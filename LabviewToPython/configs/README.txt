Configuration files (YAML/TOML) can live here later, e.g.:

- default.yaml: device mapping (COM ports), sampling rates, save paths
- profiles/*.yaml: lab-specific profiles

This scaffold does not require any config to run.
